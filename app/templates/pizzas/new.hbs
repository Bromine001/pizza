<form {{action 'save' on='submit'}}>
	<fieldset>
		<h2>Table {{view 'select' content=table value=model.table propmt='select a table'}}</h2>
		<h2>Size {{view 'select' content=size value=model.size prompt='select a size'}}</h2>
		<h2>Toppings</h2>
		{{view 'select' content=toppings value=model.topping1 prompt='select a topping'}}<br>
		{{view 'select' content=toppings value=model.topping2 prompt='select a topping'}}<br>
		{{view 'select' content=toppings value=model.topping3 prompt='select a topping'}}<br>
		{{view 'select' content=toppings value=model.topping4 prompt='select a topping'}}<br>
		<h2><input type='submit' value="Save"> <button {{action 'cancel'}}>Cancel</button></h2>
		
	</fieldset>
</form>